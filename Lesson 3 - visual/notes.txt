При использовании loc и iloc возвращается view , а не copy. Если на вход подать список колонок в loc, то вернет copy,а не view. Если нужно обратиться к нескольким столбцам, то нужно переприсвоить df=df.replace(показано на примере replace)  (надо почитать про метод inplace)

seaborn, plotly, matplotlib - для визуализации
В пандасе встроен метод plot() - основана на matplotlib
точка с запятой в конце - чтобы убрать добавление ненужной строки про matplotlib.

SEABORN

Нужно понимать, какой формат вывода (svg - лучше детализация, но медленно, можно использовать png)

sns.set() - изменяет стиль, на стиль по умолчанию (визуально приятнее)

PLOTLY

Интерактивность(динамика) графиков - главная фича. Онлайн сервис, необходимо импортировать plotly.offline
Можно сохранить как ссылку на онлайн график, можно сохранить как картинку, можно открыть  и в plotly онлайн.
Получаем Html и всё красиво и графически приятно, все данные в html верстке.

(Чекнуть bokeh модуль)

---------------------------------------------------------------------------------

Part 2 - telecom churn tsne

TSNE - используется в разведовательном анализе для сжатия N - мерного пространства до трех-двух мерных, чтобы оценить данные и связи между ними.
изменение параметров приводит к значительным изменениям в работе алгоритма, поэтому не стоит использовать результаты в последующих шагах, только для предварительного анализа.
Можно использовать для визуализации результатов анализа изображений.( очень грубо). Можно примерно оценить сколько кластеров. Т.е. для K-means.
улучшенная версия T-SNE -> LargeVis



